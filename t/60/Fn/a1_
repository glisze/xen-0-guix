a1_ () 
{ 
    local D="Touch the records of the shell function here, in Fn/.";
    for x in $( seq -w 0 099 ) $( seq -w 0 99 ) $( seq 0 9 ) $( echo $( for y in $( seq 0 9 );do echo a${y};done )) a__ c_ c__ n_ m n v c;
    do
        test $( declare -F ${x}_ ) && touch Fn/${x}_;
        test $( declare -F ${x} ) && touch Fn/${x};
    done
}
