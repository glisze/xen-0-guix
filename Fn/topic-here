topic-here () 
{ 
    local D0="being self-modifying, here; mind your step!";
    test -n "$*" && sed Fn/topic-here -e '/ D[=]/s/".*"/"'"$*"'"/' -i && . Fn/topic-here && declare -f topic-here > Fn/topic-here && topic-here;
    local D="here: Linux 5.16-rc1";
    set_sprint_topic $D
}
